# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Built release APKs, kept locally so they're easy to find and attach to a
# GitHub release — not committed, since they're large, reproducible build
# output rather than source. See README.md's APK Installation section.
/releases/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Backend (Node.js)
backend/node_modules/
backend/.env
backend/uploads/*
!backend/uploads/.gitkeep
backend/npm-debug.log*

# Editor
.vscode/
.env
*.local

# Firebase credentials — real project secrets, never committed. See
# docs/PUSH_NOTIFICATIONS.md.
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
backend/secrets/*.json

# Firebase Hosting's local deploy cache — regenerated by the CLI, not
# project config (that's .firebaserc/firebase.json, which ARE committed).
.firebase/

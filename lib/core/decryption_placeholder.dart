/// Shown in place of a message that can't be decrypted (key not ready
/// yet, or an identity change). Shared so the chat list and an open
/// thread show the same text.
const undecryptableBodyPlaceholder = '🔒 Unable to decrypt this message';
